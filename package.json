{
  "name": "research-prototype-hub",
  "version": "1.0.0",
  "description": "Research Prototype Hub",
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose build",
    "test": "pytest backend/tests/"
  }
}
